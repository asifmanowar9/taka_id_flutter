# Place your TFLite model here

After running `tools/convert_model.py`, copy the output file to:

    assets/model/banknote_classifier.tflite

This folder is intentionally committed without the model file because
.tflite files can be large. Add the file before running the app.
